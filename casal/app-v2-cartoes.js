import { createClient } from 'https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2/+esm';

const sb=createClient('https://ldgitzsdefhkbkoohkcy.supabase.co','sb_publishable_WoVp2qlQ90hTItJaxmhwEg_PFSnwvIp',{auth:{persistSession:true,autoRefreshToken:true,detectSessionInUrl:true}});
const $=id=>document.getElementById(id);
const money=v=>Number(v||0).toLocaleString('pt-BR',{style:'currency',currency:'BRL'});
const today=()=>new Date().toISOString().slice(0,10);
const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
let cards=[],purchases=[],installments=[],invoicePayments=[];
let editingCardId=null;

function addMonths(base,n){const d=new Date(base+'T12:00:00');const day=d.getDate();d.setDate(1);d.setMonth(d.getMonth()+n);const last=new Date(d.getFullYear(),d.getMonth()+1,0).getDate();d.setDate(Math.min(day,last));return d.toISOString().slice(0,10)}
function invoiceForPurchase(purchaseDate,card){const d=new Date(purchaseDate+'T12:00:00');const closing=Number(card.closing_day||31);if(d.getDate()>closing)d.setMonth(d.getMonth()+1);d.setDate(1);return d.toISOString().slice(0,10)}
function dueForInvoice(invoiceMonth,card){const d=new Date(invoiceMonth+'T12:00:00');const due=Number(card.due_day||10);const last=new Date(d.getFullYear(),d.getMonth()+1,0).getDate();d.setDate(Math.min(due,last));return d.toISOString().slice(0,10)}
function status(t,e=false){const el=$('status');if(!el)return;el.textContent=t;el.style.color=e?'var(--bad)':'var(--ok)';el.classList.remove('hidden');setTimeout(()=>el.classList.add('hidden'),7000)}
function fmtDate(d){return d?new Date(d+'T12:00:00').toLocaleDateString('pt-BR'):'-'}
function monthLabel(ymd){return new Date(ymd+'T12:00:00').toLocaleDateString('pt-BR',{month:'long',year:'numeric'})}

function ensureUI(){
  if(!document.querySelector('.nav button[data-cards-v2]')){const b=document.createElement('button');b.dataset.cardsV2='1';b.textContent='💳 Cartões';b.onclick=()=>showCards();document.querySelector('.nav')?.appendChild(b)}
  if($('cartoes'))return;
  const sec=document.createElement('section');sec.id='cartoes';sec.className='page hidden';sec.innerHTML=`<div class="split"><div class="panel"><h3 id="cardFormTitle">Novo cartão</h3><form id="cardForm" class="grid"><div class="field"><label>Nome do cartão</label><input id="cardName" required></div><div class="field"><label>Responsável</label><select id="cardOwner"><option>Igor</option><option>Larissa</option><option>Casal</option></select></div><div class="field"><label>Dia de fechamento</label><input id="cardClosing" type="number" min="1" max="31"></div><div class="field"><label>Dia de vencimento</label><input id="cardDue" type="number" min="1" max="31"></div><div class="field"><label>Limite</label><input id="cardLimit" type="number" step=".01"></div><div class="row"><button class="btn" id="cardSaveBtn">Salvar cartão</button><button type="button" class="btn alt hidden" id="cardCancelBtn">Cancelar edição</button></div></form></div><div class="panel"><h3>Cartões cadastrados</h3><div id="cardsList"></div></div></div><div class="panel"><h3>Resumo das próximas faturas</h3><div id="futureBills"></div></div><div class="panel"><h3>Adicionar compra na fatura</h3><form id="purchaseForm" class="grid"><div class="field"><label>Cartão</label><select id="purchaseCard" required></select></div><div class="field"><label>Compra</label><input id="purchaseDesc" required></div><div class="field"><label>Categoria</label><input id="purchaseCat"></div><div class="field"><label>Data da compra</label><input id="purchaseDate" type="date" required></div><div class="field"><label>Valor total da compra</label><input id="purchaseAmount" type="number" step=".01" min="0" required></div><div class="field"><label>Parcelas</label><input id="purchaseInstallments" type="number" min="1" max="120" value="1" required></div><div><button class="btn">Adicionar compra</button></div></form></div><div class="panel"><h3>Faturas por cartão</h3><div class="row"><div class="field"><label>Cartão</label><select id="invoiceCard"></select></div><div class="field"><label>Mês</label><input id="invoiceMonth" type="month"></div><button class="btn alt" id="invoiceRefresh">Atualizar</button></div><div id="invoiceTable"></div></div><div class="panel"><h3>Registrar pagamento da fatura</h3><form id="invoicePaymentForm" class="grid"><div class="field"><label>Cartão</label><select id="paymentCard" required></select></div><div class="field"><label>Fatura do mês</label><input id="paymentMonth" type="month" required></div><div class="field"><label>Valor pago</label><input id="paymentAmount" type="number" step=".01" min="0" required></div><div class="field"><label>Dia em que foi pago</label><input id="paymentDate" type="date" required></div><div class="field"><label>Comprovante</label><input id="paymentReceipt" type="file" accept="image/*,.pdf"></div><div class="field"><label>Observação</label><input id="paymentNotes"></div><div><button class="btn">Salvar pagamento</button></div></form><div id="paymentsTable"></div></div><div class="panel"><h3>Compras cadastradas</h3><div id="purchaseTable"></div></div>`;
  document.querySelector('.main')?.appendChild(sec);$('purchaseDate').value=today();const d=new Date();$('invoiceMonth').value=d.toISOString().slice(0,7);$('paymentMonth').value=d.toISOString().slice(0,7);$('paymentDate').value=today();$('cardForm').addEventListener('submit',saveCard);$('cardCancelBtn').addEventListener('click',resetCardForm);$('purchaseForm').addEventListener('submit',savePurchase);$('invoicePaymentForm').addEventListener('submit',saveInvoicePayment);$('invoiceRefresh').addEventListener('click',renderInvoice);$('invoiceCard').addEventListener('change',renderInvoice);$('invoiceMonth').addEventListener('change',renderInvoice)
}
async function loadCards(){const [c,p,i,pay]=await Promise.all([sb.from('personal_cards').select('*').order('name'),sb.from('personal_card_purchases').select('*').order('purchase_date',{ascending:false}),sb.from('personal_card_installments').select('*').order('invoice_month').order('installment_number'),sb.from('personal_card_invoice_payments').select('*').order('paid_at',{ascending:false})]);const err=[c,p,i,pay].find(x=>x.error)?.error;if(err)return status('Erro ao carregar cartões: '+err.message,true);cards=c.data||[];purchases=p.data||[];installments=i.data||[];invoicePayments=pay.data||[];renderAll()}
function renderAll(){const opts=cards.map(c=>`<option value="${c.id}">${esc(c.name)} (${esc(c.owner)})</option>`).join('');$('purchaseCard').innerHTML=opts;$('invoiceCard').innerHTML='<option value="">Todos</option>'+opts;$('paymentCard').innerHTML=opts;renderCardsList();renderFuture();renderInvoice();renderPayments();renderPurchases()}
function renderCardsList(){$('cardsList').innerHTML=cards.length?`<div class="tablewrap"><table class="table"><thead><tr><th>Cartão</th><th>Responsável</th><th>Fecha</th><th>Vence</th><th>Limite</th><th></th></tr></thead><tbody>${cards.map(c=>`<tr><td>${esc(c.name)}</td><td>${esc(c.owner)}</td><td>${c.closing_day||'-'}</td><td>${c.due_day||'-'}</td><td>${c.credit_limit?money(c.credit_limit):'-'}</td><td><button class="btn sm" onclick="window.editCard('${c.id}')">Editar</button></td></tr>`).join('')}</tbody></table></div>`:'<div class="muted">Nenhum cartão cadastrado.</div>'}
function renderFuture(){const now=new Date(),rows=[];for(let m=0;m<6;m++){const d=new Date(now.getFullYear(),now.getMonth()+m,1),key=d.toISOString().slice(0,10),total=installments.filter(x=>x.invoice_month===key&&!x.paid).reduce((a,x)=>a+Number(x.amount||0),0);rows.push(`<div style="padding:8px 0;border-bottom:1px solid var(--line)"><b>${d.toLocaleDateString('pt-BR',{month:'long',year:'numeric'})}</b><span style="float:right">${money(total)}</span></div>`)}$('futureBills').innerHTML=rows.join('')}
function renderInvoice(){const cardId=$('invoiceCard').value,month=($('invoiceMonth').value||new Date().toISOString().slice(0,7))+'-01';let rows=installments.filter(x=>x.invoice_month===month&&(!cardId||x.card_id===cardId));if(!rows.length){$('invoiceTable').innerHTML='<div class="muted">Nenhuma parcela nesta fatura.</div>';return}const total=rows.reduce((a,x)=>a+Number(x.amount||0),0);$('invoiceTable').innerHTML=`<p><b>Total: ${money(total)}</b></p><div class="tablewrap"><table class="table"><thead><tr><th>Cartão</th><th>Compra</th><th>Parcela</th><th>Valor</th><th>Vencimento</th><th>Status</th></tr></thead><tbody>${rows.map(x=>{const p=purchases.find(p=>p.id===x.purchase_id),c=cards.find(c=>c.id===x.card_id);return `<tr><td>${esc(c?.name||'-')}</td><td>${esc(p?.description||'-')}</td><td>${x.installment_number}/${x.installments_total}</td><td>${money(x.amount)}</td><td>${fmtDate(x.due_date)}</td><td>${x.paid?'Pago':'Aberto'}</td></tr>`}).join('')}</tbody></table></div>`}
function renderPayments(){$('paymentsTable').innerHTML=invoicePayments.length?`<div class="tablewrap"><table class="table"><thead><tr><th>Cartão</th><th>Fatura</th><th>Valor</th><th>Pago em</th></tr></thead><tbody>${invoicePayments.map(p=>{const c=cards.find(c=>c.id===p.card_id);return `<tr><td>${esc(c?.name||'-')}</td><td>${monthLabel(p.invoice_month)}</td><td>${money(p.amount)}</td><td>${fmtDate(p.paid_at)}</td></tr>`}).join('')}</tbody></table></div>`:''}
function renderPurchases(){$('purchaseTable').innerHTML=purchases.length?`<div class="tablewrap"><table class="table"><thead><tr><th>Data</th><th>Cartão</th><th>Compra</th><th>Valor</th><th>Parcelas</th></tr></thead><tbody>${purchases.map(p=>{const c=cards.find(c=>c.id===p.card_id);return `<tr><td>${fmtDate(p.purchase_date)}</td><td>${esc(c?.name||'-')}</td><td>${esc(p.description)}</td><td>${money(p.total_amount)}</td><td>${p.installments}x</td></tr>`}).join('')}</tbody></table></div>`:'<div class="muted">Nenhuma compra cadastrada.</div>'}
function resetCardForm(){editingCardId=null;$('cardForm').reset();$('cardFormTitle').textContent='Novo cartão';$('cardSaveBtn').textContent='Salvar cartão';$('cardCancelBtn').classList.add('hidden')}
window.editCard=id=>{const c=cards.find(x=>x.id===id);if(!c)return;editingCardId=id;$('cardName').value=c.name||'';$('cardOwner').value=c.owner||'Casal';$('cardClosing').value=c.closing_day||'';$('cardDue').value=c.due_day||'';$('cardLimit').value=c.credit_limit||'';$('cardFormTitle').textContent='Editar cartão';$('cardSaveBtn').textContent='Salvar alterações';$('cardCancelBtn').classList.remove('hidden')};
async function saveCard(e){e.preventDefault();const payload={name:$('cardName').value.trim(),owner:$('cardOwner').value,closing_day:Number($('cardClosing').value)||null,due_day:Number($('cardDue').value)||null,credit_limit:Number($('cardLimit').value)||null};const q=editingCardId?sb.from('personal_cards').update(payload).eq('id',editingCardId):sb.from('personal_cards').insert(payload);const{error}=await q;if(error)return status(error.message,true);resetCardForm();await loadCards()}
async function savePurchase(e){e.preventDefault();const card=cards.find(c=>c.id===$('purchaseCard').value);if(!card)return;const total=Number($('purchaseAmount').value),count=Number($('purchaseInstallments').value),purchaseDate=$('purchaseDate').value;const{data:p,error}=await sb.from('personal_card_purchases').insert({card_id:card.id,description:$('purchaseDesc').value.trim(),category:$('purchaseCat').value.trim()||null,purchase_date:purchaseDate,total_amount:total,installments:count}).select().single();if(error)return status(error.message,true);const first=invoiceForPurchase(purchaseDate,card),base=Math.floor(total*100/count),rem=Math.round(total*100)-base*count,rows=[];for(let n=0;n<count;n++){const month=addMonths(first,n).slice(0,7)+'-01';rows.push({purchase_id:p.id,card_id:card.id,installment_number:n+1,installments_total:count,amount:(base+(n===count-1?rem:0))/100,invoice_month:month,due_date:dueForInvoice(month,card),paid:false})}const r=await sb.from('personal_card_installments').insert(rows);if(r.error)return status(r.error.message,true);e.target.reset();$('purchaseDate').value=today();$('purchaseInstallments').value=1;await loadCards()}
async function saveInvoicePayment(e){e.preventDefault();const cardId=$('paymentCard').value,month=$('paymentMonth').value+'-01',amount=Number($('paymentAmount').value),paidAt=$('paymentDate').value;const{error}=await sb.from('personal_card_invoice_payments').insert({card_id:cardId,invoice_month:month,amount,paid_at:paidAt,notes:$('paymentNotes').value.trim()||null});if(error)return status(error.message,true);await sb.from('personal_card_installments').update({paid:true,paid_at:paidAt}).eq('card_id',cardId).eq('invoice_month',month);e.target.reset();$('paymentDate').value=today();await loadCards()}
function showCards(){document.querySelectorAll('.page').forEach(x=>x.classList.add('hidden'));$('cartoes').classList.remove('hidden');$('title').textContent='Cartões';loadCards()}
async function boot(){ensureUI();const{data:{session}}=await sb.auth.getSession();if(session)loadCards();sb.auth.onAuthStateChange((event,session)=>{if(session)loadCards()})}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot);else boot();

// Carregamento isolado: se Bancos falhar, Cartões e o restante do sistema continuam funcionando.
import('./app-v5-bancos-seguro.js').catch(err=>console.error('Falha isolada no módulo Bancos:',err));
// Correção isolada da folha: extras de um mês entram no salário do mês seguinte.
import('./app-v7-folha-salario.js').catch(err=>console.error('Falha isolada na folha salarial V7:',err));